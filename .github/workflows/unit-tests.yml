---
name: Unit Tests

on: # yamllint disable-line rule:truthy
  pull_request:
    paths:
      - dist/**
      - src/**
      - vitest.config.ts
  push:
    branches:
      - main
  workflow_dispatch: {}

defaults:
  run:
    shell: sh

jobs:
  vitest:
    name: Vitest
    uses: fabasoad/reusable-workflows/.github/workflows/wf-js-unit-tests.yml@ecce8eb77aa808e37355819f2a4ac62deefd0aff # main
    permissions:
      contents: read
    secrets: inherit # pragma: allowlist secret
