---
name: Release

on: # yamllint disable-line rule:truthy
  push:
    tags:
      - "v*.*.*"

jobs:
  github:
    name: GitHub
    uses: fabasoad/reusable-workflows/.github/workflows/wf-github-release.yml@ecce8eb77aa808e37355819f2a4ac62deefd0aff # main
    permissions:
      contents: write
